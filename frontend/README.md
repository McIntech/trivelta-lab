# frontend

Next.js 15 + TypeScript + TanStack Query. Talks to FastAPI at `localhost:8000`.

```bash
npm install
cp .env.example .env.local
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
